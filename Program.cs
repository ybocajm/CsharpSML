using System;

class Program // a grouping of code that's reusable
{
	static void Main()
	{
		Console.WriteLine("Welcome to Acme Accounting Systems");
		Console.WriteLine("Remember, we're \"accounting\" on you!");
		Console.ReadLine(); // tells console screen to stay open
	}
}
// a solution is a container for projects (solution explorer)
// a project is a grouping of files that'll be a dll or exe